# pbarnes-sit722-part5/terrafiles

This example uses Terraform to create a Kubernetes cluster in Azure.

Run this example using:

```bash
cd pbarnes-sit722-part5/terrafiles
terraform init
terraform apply
```
